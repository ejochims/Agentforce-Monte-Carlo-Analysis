<?xml version="1.0" encoding="UTF-8"?>
<!--
  MonteCarlo_API_Auth.externalCredential-meta.xml

  PURPOSE:
    External Credential is the authentication container in Salesforce's
    Named Credentials v2 architecture. It defines HOW to authenticate
    to the external service (in this case: no authentication — the service
    is open, or auth is handled at the network/VPN layer).

  TO ADD API KEY AUTHENTICATION:
    Change authenticationProtocol to "Custom" and add an externalCredentialParameter
    with parameterType "AuthHeader" containing your API key value.
    See: https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_named_cred.htm

  DEPLOY:
    sf project deploy start -m "ExternalCredential:MonteCarlo_API_Auth" --target-org <alias>
-->
<ExternalCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Monte Carlo API Auth</label>
    <authenticationProtocol>Anonymous</authenticationProtocol>
    <principals>
        <credentialName>MonteCarlo_API_Auth</credentialName>
        <parameterName>default</parameterName>
        <principalType>NamedPrincipal</principalType>
        <sequenceNumber>1</sequenceNumber>
    </principals>
</ExternalCredential>
